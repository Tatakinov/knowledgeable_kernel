＊
\0\s[素]$ ls -l `which ruby`
ってするとwhich rubyの実行結果を引数にlddが実行できるよ。
\1\s[素]内部的には
$ ls -l `which ruby`
        ↓
$ ls -l /usr/bin/ruby
が行われているってことやね。
ただ、``で囲むより$()で囲んだ方がネスト出来てええな。
\0\s[ドヤッ]\n[half]$ ls -l $(which ruby)
こうだね！_

＊
\0\s[素]bashのalias機能ってどんな時に使うの？_
\1\s[素]ワンライナーほどのことはやらんけど、いちいちコマンド打つのが面倒な時やね。
alias ll='ls -lh'
alias wine='WINEDEBUG=-all wine'
こんな感じに。
\0\n[half]複雑なことをやりたい時は？_
\1\n[half]aliasを使わずにfunctionで関数を定義するんが普通。
function mc() { mkdir $1 && cd $1; }
みたいに。

＊
\0\s[困った]コマンドの出力先をroot権限のファイルにしようと思って
$ sudo command > /path/to/output.txt
ってしたんだけど、sudoしてるのに「許可がありません」って出ちゃった…。
\1\s[素]commandにはsudoの効果がのるけど、> /path/to/output.txtの部分はシェルが処理を行っとるからsudoの効果が及ばんのや。
\n[half]$ command | sudo tee /path/to/output.txt >/dev/null
や
$ sudo sh -c 'command > /path/to/output.txt'
などとすればうまくいくはずや。

＃bashの場合。
＊
\0\s[素]コマンドの説明する時って、
$ foo input.txt
みたいに書かれてるけど、この「$」って何者？_
\1\s[素]プロンプトと呼ばれるものやね。
デフォルトは
ユーザー名@ホスト名:カレントディレクトリ$
になっていて、環境変数PS1を弄ることで変えられるで。

＊
\0\s[素]コマンドの説明で
$ foo input.txt | bar output.txt
みたいな感じで書いてある時のあの「|」とは！_
\1\s[素]パイプライン(パイプ)やね。
fooコマンドの出力結果をbarに入力している感じになるな。
\0\n[half]中間ファイルを作らずに済むので、途中で大きなファイルを作ってしまう場合なんかに有用だよ。
\1\n[half]テキスト処理をするコマンドなら大体のものはパイプに対応しているはずや。

＊
\0\s[素]ぬーさん、標準エラー出力だけパイプを使いたい場合ってどうすればいいかな？_\n
\1\s[素]$ foo 2>&1 >/dev/null | bar
とすれば出来るで。
2>&1と>/dev/nullの順番はこれやないと意味が無いから気をつけてな。

＊
\0\s[素]$ < text.txt sed -e 's/うかがか/伺か/g > text.txt'\w9\w9
text.txtの「うかがか」を「伺か」に変換してる…ように見えるけど
これだとうまく動かないんだ。
\1\s[素]リダイレクトがプログラム起動より先に行われるからやね。
\0\n[half]処理の順番としては、
1. 読み込み用、書き込み用にtext.txtを開く
2. sedを起動する
となっていて、1番の書き込み用にtext.txtを開いた時点でファイルの中身が空っぽになってしまい、2番でsedは空っぽのファイルを読み込むようになってしまうというわけ。

＊
\0\s[素]sedやperlで文字列置換を行うときの正規表現の部分は"(ダブルクオート)じゃなくて'(シングルクオート)で囲った方がいいよ。
こんな感じに。
$ sed -e 's/^SEKIRO$/SERIKO/g'\w9\w9
\1\s[素]正規表現内で\\とか$とか使うからやね。
ダブルクオートだとエスケープされたり変数展開されたりするでな。

＊
\0\s[素]シェルには特殊変数$?っていうのがあって、
直前に実行したコマンドの戻り値が入っているんだ。
\1\s[素]$ echo $?
なんかで確認出来るで。
\0\n[half]「直前の」というのがミソで、間に別のコマンドを挟んでしまうと得られる値が変わる可能性があるから注意だね。
\1\n[half]$ false
$ echo $?
$ echo $?
なんかが一例やね。

＊
\0\s[素]command1とcommand2の出力結果をcommand3で処理するときに
$ command1 > output1.txt
$ command2 > output2.txt
$ cat output1.txt output2.txt | command3
と書けるものであれば、
$ (command1; command2) | command3
と書くことが出来るよ。
\1\s[素]グループコマンドと呼ばれるやつやね。
\0\n[half]一時ファイルを作らなくて済むし見た目も綺麗だよね！_

＊
\0\s[素]コマンド名や引数にファイル名を入力するとき、TABキーを押すと補完されるのは知ってる？
\1\s[素]便利よな。
\0\n[half]その機能をさらに便利にするために！_
bash-completionをインストールしよう！_
\1\n[half]もしかしたら既に入っとるかもな。
\0\n[half]説明しよう！_bash-completionとは！_
コマンドのオプション名も補完できるようになったり、sshならリモートのホスト名が補完出来るようになったりと、通常では補完出来なかったものまで出来るようになる凄いパッケージなのだ！_
\1\n[half]systemctlとかの長ったらしいオプションもこれで一発やね。

＊
\0\s[素]カレントディレクトリのファイルを全部消す時に
$ rm -r *
とやりがちだけど、別のターミナルにフォーカスがある状態で実行してしまうと大変危険なので\s[落ち込み]注意…_しようね…_。
\1消すときは一個上のディレクトリに移動してフォルダを指定して消すようにするのがええな。

＊
\0\s[素]ディレクトリを再帰的に走査してコマンドを実行するのに
$ find . -name "*.txt" -exec command {} \\+
とするのが面倒なら
$ shopt -s globstar
しておけば
$ command **/*.txt
で同じ結果が得られるよ。
\1\s[素]毎度shoptを実行するのが面倒なら.bashrcに書いておくんやで。

＊
\0\s[素]「[](角括弧)」と「{}(波括弧)」の違いって何？_
\1\s[素]「[]」は存在するファイル/ディレクトリ名だけ展開して、「{}」は全部展開するようになってるな。
\n[half]例えば、1.txt、2.txt、3.txtとファイルがあったときに、
$ echo [12345].txt
は
1.txt 2.txt 3.txt
と出力されて、
$ echo {1,2,3,4,5}.txt
は
1.txt 2.txt 3.txt 4.txt 5.txt
と出力されるんや。

＊
\0\s[素]カレントディレクトリにあるコマンドって実行する時に
$ ./hoge\w9\w9
みたいに「./」をつけないと実行出来ないのってなんでだろ？_
\1\s[素]「./」をつけずに実行出来てしまうと、特にスクリプトなんかで、実行結果がカレントディレクトリにあるコマンドに左右されかねないからとちゃうか。
あと、「./」をつけなくても実行できてしまうと、逆にPATH内にあるコマンドを呼び出したい時にいちいち
$ /usr/bin/command ...\w9\w9
みたいにしないといけない方が面倒ってのもあるかもな。
\0\s[おこ]\n[half]むむむ、たしかにそれは嫌かも。\n

＊
\0\s[素]txtファイルだけコマンドの引数に与えたい時に
$ command *.txt\w9\w9
なんてすることがあるけど、その「*」のことをワイルドカードと呼ぶよ。
\1\s[素]「?」が任意の一文字、「*」が任意の長さ0以上の文字列に対応するで。
\0\n[half]「[]」や「{}」とは同時に使えないからうまく使い分けようね！_

＊
\0\s[素]ワイルドカードを展開せずにコマンドの引数に渡したい時はクオートで囲むかバックスラッシュをつければOKだよ。
\1\s[素」ワイルドカード以外に、「-」「!」「 」辺りも同じやね。
実用上はクオートで囲むことが多いんとちゃうかな。

＊
\0\s[素]$ command1 |& command2
とすると、command1の標準出力、標準エラー出力の両方を入力として
command2を実行することが出来るよ。
\1\s[素]$ command1 2>&1 | command2
と等価やね。
\0\n[half]どんな時に使うの？
\1\n[half]コンパイルエラーがアホみたいに長いとき。
\0\n[half]あーね。仮想端末の履歴より長いことあるもんね。

